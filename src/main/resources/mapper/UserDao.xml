<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wlx.demo.dao.UserDao">
    <insert id="adduser" parameterType="map">
        INSERT  INTO wlx_user (username,password,adress)VALUES (#{username},#{password},#{adress})
    </insert>
</mapper>